/*
package com.laio;

import io.quarkus.test.junit.QuarkusTest;
import org.junit.jupiter.api.Test;

import static io.restassured.RestAssured.given;

@QuarkusTest
public class EpisodeControllerTest {

    @Test
    public void testGetAllEpisodeEndpoint() {
        given()
          .when().get("/episode")
          .then()
             .statusCode(200);
    }


    @Test
    public void testGetEpisodeEndpoint() {
        given()
                .when().get("/episode/1")
                .then()
                .statusCode(200);
    }
}*/
